#ifndef PUSH_H      // Старий класичний спосіб (називається Include Guard): Якщо PUSH_H ще не визначено...
#define PUSH_H      // ...визначити PUSH_H (запам'ятати, що цей файл уже був включений)

unsigned char plus();
unsigned char minus();
// інші оголошення...
unsigned char uvod ();
unsigned char Entrance ();
//void button ();

unsigned char button (char x);
//unsigned char button ();
unsigned char long_button (char x);

#endif  // Кінець умовного включення






